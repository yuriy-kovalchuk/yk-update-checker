# This project is configured via CLI flags and a YAML config file (see examples/config.yaml).
# No environment variables are required to run the binary.
#
# Credentials for private Git repos are passed via file paths in the YAML config
# (token_file, password_file, ssh_key_path) and sourced from Kubernetes Secrets at runtime.
