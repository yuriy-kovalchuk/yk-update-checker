apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "yk-update-checker.fullname" . }}-config
  labels:
    {{- include "yk-update-checker.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.scanner.config | toYaml | nindent 4 }}
